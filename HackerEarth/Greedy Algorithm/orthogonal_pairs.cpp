#include<bits/stdc++.h>

typedef long long int lli;

#define loop(i, a, b) \
for(lli i=a; i<b; i++)

int main(int argc, const char *argv[]){
    lli N;
    scanf("%llu", &N);
    lli *X, *Y;
    X = (lli*)malloc(sizeof(lli)*N);
    Y = (lli*)malloc(sizeof(lli)*N);
    memset(X, 0, sizeof(lli)*N);
    memset(Y, 0, sizeof(lli)*N);
    loop(i, 0, N){
        loop(j, i, N){
            loop(k, )
        }
    }
    return 0;
}
